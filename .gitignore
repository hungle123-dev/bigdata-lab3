# ---- Dữ liệu ----
# asr.csv (bản đã clean bằng clean.ipynb) ĐƯỢC commit để nhóm chạy trực tiếp.
# Chỉ chặn bản gốc chưa xử lý + file tạm.
data/Amazon Sale Report.csv
data/*_backup.csv

# ---- Kết quả xuất (build lại được, không commit) ----
out/

# ---- Maven / JVM ----
target/
*.class
dependency-reduced-pom.xml

# ---- LaTeX build tạm ----
report/*.aux
report/*.log
report/*.out
report/*.toc
report/*.synctex.gz

# ---- OS / editor ----
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/
*.swp
*~

# ---- File thừa (CSV gốc chưa xử lý + rác lab khác) ----
Lab03/Amazon Sale Report.csv
shapes.parquet(legacy)
